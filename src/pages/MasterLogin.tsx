export default function MasterLogin() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center bg-ink p-8 text-center text-white">
      <h1 className="font-serif text-3xl font-semibold">Master Editor</h1>
      <p className="mt-2 max-w-sm text-white/60">
        Admin login lands here in Phase 8, backed by a dedicated admin-login Edge Function
        (see the rebuild plan for the auth design).
      </p>
    </div>
  );
}
